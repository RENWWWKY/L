# 本地 npm run dev 专用（Vite 自动加载，不会进入生产构建）
# 默认跳过账号状态检测，方便日常开发；要测登录/检测流程时改为 false
VITE_LOCAL_DEV_BYPASS_AUTH=true

# 本地向量模型 · dev 自动走 /hf-proxy，无需配置

# 账号 API（与 GitHub Pages 生产一致；本地调试 Worker 时可改成本地 wrangler dev 地址）
VITE_USER_SYSTEM_API_BASE=https://user-system-api.lyx815934990.workers.dev
