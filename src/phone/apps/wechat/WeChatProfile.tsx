/**
 * 微信「我」页面（Profile）。
 * UI 实现在 `WeChatMeInstagramProfile`，此处为模块命名对齐与聚合导出。
 */
export {
  WeChatMeInstagramProfile,
  type WeChatMeInstagramProfileProps,
  type MenuRowId,
} from '../../../components/WeChatMeInstagramProfile'

/** 规格文档别名 */
export { WeChatMeInstagramProfile as WeChatProfile } from '../../../components/WeChatMeInstagramProfile'
