/**
 * 线下约会内心 OS 标记：渲染层只认半角 `**…**`。
 * 不在展示层做「…… / ... → **」全局替换，以免误伤对白里的省略号（如「……算了。」）。
 */

/** 注入 system / 思维链：明确半角双星号，禁止省略号起止符 */
export const DATING_INNER_OS_MARKUP_RULE =
  '内心 OS 须用半角双星号起止包裹，格式：**此处写第一人称心思**（英文 * 连按两次；**禁止**用 ……、… 或 ... 代替星号作起止符）'
